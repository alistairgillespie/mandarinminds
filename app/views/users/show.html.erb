<!-- <p id="notice"><%= notice %></p> -->
		
			<div class="row">
				<div class="col-sm-3"><%= image_tag current_user.gravatar_url(:size => 175), :class => 'img-circle' %> </div>
				<div class="col-sm-9"><h1>Welcome</h1>
					<h2><%= @user.email %></h2><br>
					<%= Time.now.strftime("%I:%M:%S") %></div>
			</div>
				<div>
				Your schedule for the next week:
				<div class="row profile-calendar-div">
			
				<div class="col-md-1 profile-calendar-box">
					<% @date = -1.day.from_now %>
					<%= @date.strftime("%A")%></br>
					<%= @date.strftime("%-d/%-m/%y")%>
					<% @user.lessons_to_attend.each do |l| %>
			   			Lesson    
					<% end %>
				</div>
				<div class="col-md-1 profile-calendar-box today">
					<% @date = Time.zone.now %>
					<%= @date.strftime("%A")%></br>
					<%= @date.strftime("%-d/%-m/%y")%>
				</div>
				<div class="col-md-1 profile-calendar-box">	
					<% @date = 1.day.from_now %>
					<%= @date.strftime("%A")%></br>
					<%= @date.strftime("%-d/%-m/%y")%>
				</div>
				<div class="col-md-1 profile-calendar-box">	
					<% @date = 2.day.from_now %>
					<%= @date.strftime("%A")%></br>
					<%= @date.strftime("%-d/%-m/%y")%>
				</div>
				<div class="col-md-1 profile-calendar-box">	
					<% @date = 3.day.from_now %>
					<%= @date.strftime("%A")%></br>
					<%= @date.strftime("%-d/%-m/%y")%>
				</div>
				<div class="col-md-1 profile-calendar-box">	
					<% @date = 4.day.from_now %>
					<%= @date.strftime("%A")%></br>
					<%= @date.strftime("%-d/%-m/%y")%>	</div>
				<div class="col-md-1 profile-calendar-box">	
					<% @date = 5.day.from_now %>
					<%= @date.strftime("%A")%></br>
					<%= @date.strftime("%-d/%-m/%y")%>
				</div>
				<div class="col-md-1 profile-calendar-box">	
					<% @date = 6.day.from_now %>
					<%= @date.strftime("%A")%></br>
					<%= @date.strftime("%-d/%-m/%y")%>
				</div>
				</div>
			</div>
		
<!-- Close of user inner -->
		</div>
	</div>	
</div>








